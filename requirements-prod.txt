# Runtime deps for the Docker image.
#
# NOTE: The repo-root requirements.txt is a UTF-16 `pip freeze` from a Windows
# venv and includes pywin32. Do not use it for the Linux Docker build — use
# this file instead.
#
# Versions match the dev freeze where the package is a direct import.
# pip resolves transitive deps (starlette, eth-abi, hexbytes, jiter, etc.).

fastapi==0.135.3
uvicorn[standard]==0.44.0
httpx==0.28.1
python-dotenv==1.2.2
pydantic==2.12.5
web3==7.15.0
eth-account==0.13.7
anthropic==0.93.0

# Phala TDX attestation SDK. Optional in dev (tee.py falls back to mock),
# required inside the CVM for real DCAP quotes.
dstack-sdk==0.5.3
